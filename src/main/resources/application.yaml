spring:
  datasource:
    url: ${FINGERPRINT_DB_URL}
    username: ${FINGERPRINT_DB_USERNAME}
    password: ${FINGERPRINT_DB_PASSWORD}
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect